# engine-startup-agenda-2022

Install dependencies: `npm i`

Run locally: `npm run serve`

Build: `npm run build`

Deploy to firebase: `firebase deploy`

Deploy to github: git subtree push --prefix dist origin gh-pages
